spring:
  application:
    name: gagent

  # ── PostgreSQL (uncomment when DB is ready, and remove excludes from GagentApplication.java) ──
  # datasource:
  #   url: jdbc:postgresql://localhost:5432/gagent
  #   username: ${DB_USERNAME:postgres}
  #   password: ${DB_PASSWORD:postgres}
  #   driver-class-name: org.postgresql.Driver
  # jpa:
  #   hibernate:
  #     ddl-auto: none
  #   show-sql: false
  #   properties:
  #     hibernate:
  #       dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
